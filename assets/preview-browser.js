(()=>{
'use strict';
const CFG={owner:'nepheris',repo:'nLab-Webmaster-Preview',branch:'main',root:'Preview-Web-Sandbox'};
const API=`https://api.github.com/repos/${CFG.owner}/${CFG.repo}`;
const K={inventory:'nlab-preview-inventory-v1',reviews:'nlab-preview-reviews-v1',prefs:'nlab-preview-prefs-v1'};
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
let inventory=[], sort={key:'date',dir:'desc'}, currentReviewKey=null;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const seg=s=>encodeURIComponent(s).replace(/%2F/gi,'/');
const pageUrl=(project,iteration,file='index.html')=>`${CFG.root}/${seg(project)}/${seg(iteration)}/${seg(file)}`;
const ghUrl=(project,iteration='')=>`https://github.com/${CFG.owner}/${CFG.repo}/tree/${CFG.branch}/${CFG.root}/${seg(project)}${iteration?'/'+seg(iteration):''}`;
const hash=s=>{let h=0;for(const c of s)h=(h*31+c.charCodeAt(0))>>>0;return h};
const color=s=>`hsl(${hash(s)%360} 62% 48%)`;
const icon=(id,cls='icon')=>`<svg class="${cls}" aria-hidden="true"><use href="#${id}"></use></svg>`;
function prefs(){try{return {...{theme:'light',view:'gallery',card:320},...JSON.parse(localStorage.getItem(K.prefs)||'{}')}}catch{return {theme:'light',view:'gallery',card:320}}}
function savePrefs(p){localStorage.setItem(K.prefs,JSON.stringify(p))}
function reviews(){try{return JSON.parse(localStorage.getItem(K.reviews)||'{}')}catch{return {}}}
function setReviews(v){localStorage.setItem(K.reviews,JSON.stringify(v))}
function defaultReview(){return {decision:'to_review',retention:'temporary',priority:'normal',rating:0,favorite:false,protected:false,comment:'',updated_at:null}}
function reviewFor(key){return {...defaultReview(),...(reviews()[key]||{})}}
function labelDecision(v){return {to_review:'À examiner',rework:'À retravailler',validated:'Validé',rejected:'Rejeté / échec'}[v]||v}
function labelRetention(v){return {temporary:'Temporaire',next_validation:'Jusqu’à la prochaine validation',until_poc:'Jusqu’à validation POC',until_architecture:'Jusqu’à validation Architecture',until_development:'Jusqu’à validation Développement',until_tests:'Jusqu’à validation Tests',until_human:'Jusqu’à validation HUMAN',until_delivery:'Jusqu’à Livraison / fin du projet',archive:'À archiver',delete:'À supprimer'}[v]||v}
function reviewBadge(r){const cls={validated:'ok',rework:'warn',rejected:'bad',to_review:'muted'}[r.decision]||'muted';return `<span class="badge ${cls}">${esc(labelDecision(r.decision))}</span>${r.favorite?'<span class="badge star">★ Référence</span>':''}${r.retention==='delete'?'<span class="badge bad">À supprimer</span>':''}`}
async function api(path){const r=await fetch(API+path,{headers:{Accept:'application/vnd.github+json'},cache:'no-store'});if(!r.ok)throw new Error(`GitHub API ${r.status}`);return r.json()}
function parseIteration(folder){const m=folder.match(/^(\d{8})_(\d{6})(?:_(.*))?$/);if(!m)return {stamp:null,date:null,label:folder};const d=m[1],t=m[2],label=(m[3]||folder).replaceAll('_',' ');const iso=`${d.slice(0,4)}-${d.slice(4,6)}-${d.slice(6,8)}T${t.slice(0,2)}:${t.slice(2,4)}:${t.slice(4,6)}`;return {stamp:d+t,date:new Date(iso),label}}
async function scanProject(dir){const data=await api(`/git/trees/${dir.sha}?recursive=1`);const first=data.tree.filter(x=>x.type==='tree'&&!x.path.includes('/'));
 return first.map(it=>{const meta=parseIteration(it.path);const prefix=it.path+'/';const files=data.tree.filter(x=>x.type==='blob'&&x.path.startsWith(prefix)).map(x=>x.path.slice(prefix.length));const entry=files.includes('index.html')?'index.html':null;return {project:dir.name,iteration:it.path,label:meta.label,stamp:meta.stamp,date:meta.date?meta.date.toISOString():null,files,files_count:files.length,entry,key:`${dir.name}/${it.path}`}})}
async function scan(){const roots=await api(`/contents/${seg(CFG.root)}?ref=${encodeURIComponent(CFG.branch)}`);const dirs=roots.filter(x=>x.type==='dir'&&!x.name.startsWith('.'));const groups=await Promise.all(dirs.map(scanProject));return groups.flat().sort((a,b)=>(b.stamp||b.iteration).localeCompare(a.stamp||a.iteration))}
function cached(){try{return JSON.parse(localStorage.getItem(K.inventory)||'null')}catch{return null}}
function cache(data){localStorage.setItem(K.inventory,JSON.stringify({fetched_at:new Date().toISOString(),data}))}
async function load(force=false){const c=cached();if(c?.data?.length){inventory=c.data;render();setStatus(`Cache ${fmtDate(c.fetched_at)} · actualisation GitHub…`,'loading')}else setStatus('Lecture de Preview-Web-Sandbox…','loading');
 try{inventory=await scan();cache(inventory);render();setStatus(`${projectCount()} projets · ${inventory.length} previews · GitHub ${fmtDate(new Date().toISOString())}`,'ok')}catch(e){console.error(e);if(c?.data?.length){inventory=c.data;render();setStatus(`GitHub indisponible — cache ${fmtDate(c.fetched_at)}`,'warn')}else setStatus(`Inventaire indisponible : ${e.message}`,'bad')}}
function projectCount(){return new Set(inventory.map(x=>x.project)).size}
function fmtDate(v){if(!v)return 'n.d.';const d=new Date(v);return isNaN(d)?v:new Intl.DateTimeFormat('fr-FR',{dateStyle:'short',timeStyle:'short'}).format(d)}
function ageDays(v){if(!v)return null;return Math.max(0,Math.floor((Date.now()-new Date(v))/86400000))}
function groupProjects(){const m=new Map();for(const p of inventory){if(!m.has(p.project))m.set(p.project,[]);m.get(p.project).push(p)}for(const a of m.values())a.sort((x,y)=>(y.stamp||y.iteration).localeCompare(x.stamp||x.iteration));return [...m.entries()].sort((a,b)=>a[0].localeCompare(b[0],'fr'))}
function render(){renderGallery();renderTable();renderExplorer();renderDashboard();applyView(prefs().view);updateCounters()}
function updateCounters(){const el=$('#inventoryCounters');if(el)el.textContent=`${projectCount()} projets · ${inventory.length} previews`}
function renderGallery(){const host=$('#gallery');if(!host)return;const groups=groupProjects();host.innerHTML=groups.length?groups.map(([name,items])=>{const latest=items[0],r=reviewFor(latest.key),h=color(name);return `<article class="project-card" style="--project:${h}"><header><span class="project-dot"></span><div><h2>${esc(name)}</h2><small>${items.length} preview${items.length>1?'s':''} disponible${items.length>1?'s':''}</small></div></header><div class="latest"><span class="eyebrow">Dernière preview</span><strong>${esc(latest.label)}</strong><span>${fmtDate(latest.date)} · ${latest.files_count} fichier${latest.files_count>1?'s':''}</span>${reviewBadge(r)}<div class="actions">${latest.entry?`<a class="primary" target="_blank" rel="noopener" href="${pageUrl(name,latest.iteration)}">${icon('i-external')}Ouvrir</a>`:'<button disabled>index.html absent</button>'}<button data-review="${esc(latest.key)}">${icon('i-edit')}Review</button></div></div><details><summary>Toutes les previews (${items.length})</summary><div class="version-list">${items.map(v=>versionLine(v)).join('')}</div></details></article>`}).join(''):'<div class="empty">Aucune preview détectée dans Preview-Web-Sandbox.</div>';bindReviewButtons()}
function versionLine(v){const r=reviewFor(v.key);return `<div class="version-line"><div><b>${esc(v.label)}</b><small>${fmtDate(v.date)} · ${esc(v.iteration)}</small></div><div class="version-state">${reviewBadge(r)}</div><div class="actions">${v.entry?`<a target="_blank" rel="noopener" href="${pageUrl(v.project,v.iteration)}">${icon('i-external')}Ouvrir</a>`:'<span class="badge bad">index absent</span>'}<button data-review="${esc(v.key)}">${icon('i-edit')}Review</button></div></div>`}
function filtered(){const global=($('#globalSearch')?.value||'').trim().toLowerCase();const fs={};$$('[data-col-filter]').forEach(i=>fs[i.dataset.colFilter]=(i.value||'').trim().toLowerCase());let rows=inventory.filter(v=>{const r=reviewFor(v.key);const data={project:v.project,iteration:v.label,date:fmtDate(v.date),files:String(v.files_count),review:labelDecision(r.decision),priority:r.priority};if(global&&!Object.values(data).join(' ').toLowerCase().includes(global))return false;return Object.entries(fs).every(([k,q])=>!q||String(data[k]||'').toLowerCase().includes(q))});rows.sort((a,b)=>{const ra=reviewFor(a.key),rb=reviewFor(b.key);const val=x=>sort.key==='project'?x.project:sort.key==='iteration'?x.label:sort.key==='date'?(x.stamp||''):sort.key==='files'?x.files_count:sort.key==='review'?labelDecision(x===a?ra:rb):sort.key==='priority'?(x===a?ra.priority:rb.priority):'';const av=val(a),bv=val(b);return (typeof av==='number'?av-bv:String(av).localeCompare(String(bv),'fr',{numeric:true}))*(sort.dir==='asc'?1:-1)});return rows}
function renderTable(){const body=$('#previewTableBody');if(!body)return;body.innerHTML=filtered().map(v=>{const r=reviewFor(v.key),h=color(v.project);return `<tr><td data-label="Projet"><span class="project-pill" style="--project:${h}"><i></i>${esc(v.project)}</span></td><td data-label="Itération"><b>${esc(v.label)}</b><small>${esc(v.iteration)}</small></td><td data-label="Date">${fmtDate(v.date)}</td><td data-label="Fichiers">${v.files_count}</td><td data-label="Review">${reviewBadge(r)}</td><td data-label="Priorité">${esc(r.priority)}</td><td data-label="Actions"><div class="actions">${v.entry?`<a target="_blank" rel="noopener" href="${pageUrl(v.project,v.iteration)}">${icon('i-external')}Ouvrir</a>`:''}<button data-review="${esc(v.key)}">${icon('i-edit')}Review</button></div></td></tr>`}).join('');bindReviewButtons()}
function renderExplorer(){const host=$('#explorer');if(!host)return;host.innerHTML=`<details open class="tree-root"><summary>${icon('i-folder')} ${esc(CFG.root)}</summary>${groupProjects().map(([name,items])=>`<details><summary>${icon('i-folder')} ${esc(name)}</summary>${items.map(v=>`<details><summary>${icon('i-folder')} ${esc(v.iteration)}</summary>${v.files.map(f=>`<div class="file-row">${icon(f.endsWith('.html')?'i-code':'i-file')}<span>${esc(f)}</span>${f==='index.html'?`<a target="_blank" rel="noopener" href="${pageUrl(name,v.iteration,f)}">Ouvrir</a>`:''}</div>`).join('')}</details>`).join('')}</details>`).join('')}</details>`}
function renderDashboard(){const host=$('#dashboard');if(!host)return;const rs=reviews(), vals=Object.values(rs);const groups=groupProjects();const counts={validated:0,rework:0,delete:0,archive:0,protected:0};for(const r of vals){if(r.decision==='validated')counts.validated++;if(r.decision==='rework')counts.rework++;if(r.retention==='delete')counts.delete++;if(r.retention==='archive')counts.archive++;if(r.protected)counts.protected++}
 host.innerHTML=`<div class="kpis"><div><b>${projectCount()}</b><span>Projets</span></div><div><b>${inventory.length}</b><span>Previews</span></div><div><b>${counts.validated}</b><span>Validées</span></div><div><b>${counts.rework}</b><span>À retravailler</span></div><div><b>${counts.delete}</b><span>À supprimer</span></div><div><b>${counts.archive}</b><span>À archiver</span></div></div><div class="dash-table"><table><thead><tr><th>Projet</th><th>Début preview</th><th>Âge</th><th>Essais</th><th>Dernière</th><th>Signal</th></tr></thead><tbody>${groups.map(([name,items])=>{const chrono=[...items].sort((a,b)=>(a.stamp||a.iteration).localeCompare(b.stamp||b.iteration)),first=chrono[0],last=items[0],age=ageDays(first.date),signal=(items.length>=10?'⚠ beaucoup d’itérations':age!=null&&age>=14?'⚠ ancien':'OK');return `<tr><td>${esc(name)}</td><td>${fmtDate(first.date)}</td><td>${age==null?'n.d.':age+' j'}</td><td>${items.length}</td><td>${esc(last.label)}</td><td>${signal}</td></tr>`}).join('')}</tbody></table></div>`}
function bindReviewButtons(){$$('[data-review]').forEach(b=>b.onclick=()=>openReview(b.dataset.review))}
function openReview(key){const p=inventory.find(x=>x.key===key);if(!p)return;currentReviewKey=key;const r=reviewFor(key);$('#reviewTitle').textContent=`Review · ${p.project} · ${p.label}`;$('#reviewProject').textContent=p.project;$('#reviewIteration').textContent=p.iteration;$('#reviewDecision').value=r.decision;$('#reviewRetention').value=r.retention;$('#reviewPriority').value=r.priority;$('#reviewRating').value=String(r.rating||0);$('#reviewFavorite').checked=!!r.favorite;$('#reviewProtected').checked=!!r.protected;$('#reviewComment').value=r.comment||'';$('#reviewDialog').showModal()}
function saveReview(){if(!currentReviewKey)return;const all=reviews();all[currentReviewKey]={decision:$('#reviewDecision').value,retention:$('#reviewRetention').value,priority:$('#reviewPriority').value,rating:Number($('#reviewRating').value||0),favorite:$('#reviewFavorite').checked,protected:$('#reviewProtected').checked,comment:$('#reviewComment').value.trim(),updated_at:new Date().toISOString(),updated_by:'HUMAN'};setReviews(all);$('#reviewDialog').close();render();toast('Review enregistrée localement')}
function buildExport(){const items=inventory.map(p=>{const r=reviewFor(p.key);return {project_id:p.project,preview_id:p.iteration,entrypoint:p.entry,path:`${CFG.root}/${p.project}/${p.iteration}`,files_count:p.files_count,lifecycle:{decision:r.decision,retention_policy:r.retention,target_phase:retentionPhase(r.retention)},human_validation:{priority:r.priority,rating:r.rating,favorite:r.favorite,protected:r.protected,comment:r.comment,updated_at:r.updated_at,updated_by:r.updated_by||null},relations:[]}});return {metadonnees:{schema_version:'1.0.0-candidate',identification:{family:'PREVIEW_REVIEW',id:'PREVIEW-REVIEW-LOCAL',slug:'webmaster-preview-reviews',label_name:'nLab Webmaster Preview — Reviews',description:'Export local provisoire aligné sur le vocabulaire nLab / futur Project Master.'},lifecycle:{status:'CANDIDATE',created_at:new Date().toISOString(),updated_at:new Date().toISOString()},location:{canonical:{type:'git',repo:`${CFG.owner}/${CFG.repo}`,path:CFG.root}},publication:{status:'private-local-export'},version:{active:'1'}},contenu:{source:{viewer:'nLab Webmaster Preview',repository:`${CFG.owner}/${CFG.repo}`,branch:CFG.branch,root:CFG.root},reviews:items},dictionnaire_donnees:{mode:'CANDIDATE',schema_ref:'Project Master / JSON Studio — en cours de validation',comment:'Structure provisoire : même orientation metadonnees/contenu/dictionnaire_donnees. À ajuster après validation du référentiel Project Master.'}}}
function retentionPhase(v){return {until_poc:'POC',until_architecture:'ARCHITECTURE',until_development:'DEVELOPMENT',until_tests:'TESTS',until_human:'HUMAN_VALIDATION',until_delivery:'DELIVERY'}[v]||null}
function downloadJson(){const data=buildExport(),blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`nlab-preview-reviews-${new Date().toISOString().slice(0,19).replaceAll(':','')}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
async function importJson(file){try{const data=JSON.parse(await file.text()),rows=data?.contenu?.reviews;if(!Array.isArray(rows))throw new Error('contenu.reviews absent');const all=reviews();for(const x of rows){const key=`${x.project_id}/${x.preview_id}`,h=x.human_validation||{},l=x.lifecycle||{};all[key]={decision:l.decision||'to_review',retention:l.retention_policy||'temporary',priority:h.priority||'normal',rating:h.rating||0,favorite:!!h.favorite,protected:!!h.protected,comment:h.comment||'',updated_at:h.updated_at||null,updated_by:h.updated_by||'HUMAN'}}setReviews(all);render();toast('Reviews importées')}catch(e){alert(`Import impossible : ${e.message}`)}}
function applyView(view){const p=prefs();p.view=view;savePrefs(p);$$('[data-view]').forEach(x=>x.hidden=x.dataset.view!==view);$$('[data-view-btn]').forEach(x=>x.classList.toggle('active',x.dataset.viewBtn===view))}
function setTheme(theme){document.documentElement.dataset.theme=theme;const p=prefs();p.theme=theme;savePrefs(p);$('#themeToggle').setAttribute('aria-pressed',theme==='dark')}
function setStatus(text,kind){const e=$('#statusText');if(e){e.textContent=text;e.dataset.kind=kind}}
function toast(text){const e=$('#toast');e.textContent=text;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),1800)}
function bindResizers(){$$('.col-resizer').forEach(r=>r.onpointerdown=e=>{e.preventDefault();const th=r.parentElement,start=e.clientX,w=th.offsetWidth;const move=x=>th.style.width=Math.max(80,w+x.clientX-start)+'px';const up=()=>{removeEventListener('pointermove',move);removeEventListener('pointerup',up)};addEventListener('pointermove',move);addEventListener('pointerup',up)})}
function init(){const p=prefs();setTheme(p.theme);document.documentElement.style.setProperty('--card-min',`${p.card}px`);$('#cardSize').value=p.card;$$('[data-view-btn]').forEach(b=>b.onclick=()=>applyView(b.dataset.viewBtn));$('#themeToggle').onclick=()=>setTheme(document.documentElement.dataset.theme==='dark'?'light':'dark');$('#refreshBtn').onclick=()=>load(true);$('#globalSearch').oninput=renderTable;$$('[data-col-filter]').forEach(i=>i.oninput=renderTable);$$('[data-sort]').forEach(h=>h.onclick=()=>{const k=h.dataset.sort;sort={key:k,dir:sort.key===k&&sort.dir==='asc'?'desc':'asc'};renderTable()});$('#cardSize').oninput=e=>{document.documentElement.style.setProperty('--card-min',`${e.target.value}px`);const q=prefs();q.card=Number(e.target.value);savePrefs(q)};$('#reviewSave').onclick=saveReview;$('#reviewClose').onclick=()=>$('#reviewDialog').close();$('#exportReviews').onclick=downloadJson;$('#importReviews').onclick=()=>$('#importFile').click();$('#importFile').onchange=e=>e.target.files[0]&&importJson(e.target.files[0]);$('#saveLocal').onclick=()=>toast('État local enregistré');bindResizers();applyView(p.view);load()}
document.addEventListener('DOMContentLoaded',init);
})();